{
  "name": "opencode-herdr-orchestration",
  "version": "0.1.0",
  "description": "Capability-separated Herdr orchestration agents for OpenCode",
  "type": "module",
  "exports": "./src/index.js",
  "bin": {
    "opencode-herdr-orchestration": "./bin/orchestration.js"
  },
  "files": [
    "bin",
    "hooks",
    "src",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "test": "node --test",
    "check": "node --check src/index.js && node --check src/agents.js && node --check src/prompts.js && node --check bin/orchestration.js"
  },
  "engines": {
    "node": ">=20"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": ">=1.3.0"
  },
  "keywords": [
    "opencode",
    "herdr",
    "orchestration",
    "agents"
  ],
  "license": "MIT"
}
